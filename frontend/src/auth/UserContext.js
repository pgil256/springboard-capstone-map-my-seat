import React from "react";

//Creates context for user in app; renders html and fetches data based on specific user credentials
const UserContext = React.createContext();

export default UserContext;
